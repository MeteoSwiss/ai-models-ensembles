% Inference cost + environmental footprint table for the 7-way baseline
% comparison. Numbers derived from sacct (CSCS Clariden, Alps cluster, NVIDIA
% GH200 nodes) and from the production-baseline run accounting (each baseline:
% 112 inits x 10 members = 1120 forecasts at 15-day (360 h) lead).
% Training-cost column removed 2026-06-19 (review): not comparable across the
% different hardware/software/accounting of each source; moved to prose.
% CS h recomputed for the 10-member case-study ensemble (was 50).
%
% Required packages (already in main preamble per experiments_tables.tex):
%   amsmath, booktabs, xcolor[table], array
%
% Reference via \ref{tab:inference-cost}.

\begin{sidewaystable}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{5pt}
  \renewcommand{\arraystretch}{1.15}
  \caption{Per-baseline inference cost and footprint on CSCS Alps (NVIDIA
           GH200; 1 node = 4 superchips) for the 1120-forecast production
           workload (112 inits $\times$ 10 members, 360\,h). s/mbr is the
           median pure-inference time per member; CS h is the single-node
           wall-clock for one 10-member case-study ensemble. Atlas is
           $\sim$18-40$\times$ slower per member than the other six, a
           consequence of its multi-step diffusion sampler. \textbf{Bold}
           marks the fastest and slowest per-member time. Training cost is
           omitted and discussed in the text, since the reported figures
           are not comparable across the different hardware and accounting
           of each source.}
  \label{tab:inference-cost}
  \begin{tabular}{@{}l l r r r r r r r@{}}
    \toprule
    \textbf{Role}
        & \textbf{Model}
        & \textbf{Params}
        & \textbf{Mem$^{\#}$}
        & \textbf{s/mbr$^{\dag}$}
        & \textbf{CS h$^{\S}$}
        & \textbf{NH$^{\ddag}$}
        & \textbf{CHF$^{\ddag}$}
        & \textbf{kWh$^{\ddag}$} \\
     &  & & \textbf{(GB)} &  &  &  &  &  \\
    \midrule

    \rowcolor{phaseBg}
    \multicolumn{9}{@{}l}{\textbf{Post-hoc weight perturbation} (this work) - \emph{zero marginal training cost}} \\
    & \mbadge{sfnoC}{sfno\_modes10}       & 573 M  & 2.3  & \textbf{59}   & 0.04  & 15.3 & 44.1  & 73.4  \\
    \rowcolor{altRow}
    & \mbadge{auroraC}{aurora\_encoder}   & 1.3 B  & 5.2  & 90            & 0.06  & 23.0 & 66.2  & 110.4 \\
    & \mbadge{graphcastC}{graphcast\_all}  & 37 M   & 0.15 & 125           & 0.09  & 28.7 & 82.7  & 137.8 \\
    \rowcolor{altRow}
    & \mbadge{aifsC}{aifs\_perturbed}$^{\star\star}$ & 255 M & 1.0 & 75 & 0.05 & 18.3 & 52.7 & 87.8 \\
    \hline

    \rowcolor{phaseBg}
    \multicolumn{9}{@{}l}{\textbf{Trained probabilistic}} \\
    & \mbadge{aifsensC}{AIFS-ENS}       & 229 M  & 0.9   & 75            & 0.05  & 18.3 & 52.7  & 87.8  \\
    \rowcolor{altRow}
    & \mbadge{fcn3C}{fcn3}               & 711 M  & 2.8   & 114           & 0.08  & 26.6 & 76.6  & 127.7 \\
    & \mbadge{atlasC}{atlas}             & 4.2 B  & 17    & \textbf{2280} & 1.58  & 43.5 & 125.3 & 208.8 \\
    \hline

    \rowcolor{phaseBg}
    \multicolumn{9}{@{}l}{\textbf{Classical reference} (off-site)} \\
    & \mbadge{ifsC}{ifs\_ens}          & -    & -   & \multicolumn{5}{l}{WeatherBench-2 download only; no CSCS compute} \\

    \bottomrule
  \end{tabular}
  \vspace{2pt}

  \scriptsize
  $^{\#}$\,Loaded model size at fp32 ($\sim$param count $\times$ 4 bytes;
  Aurora ckpt file is 4.68 GB at fp32; AIFS .ckpt is anemoi-packaged so
  the raw param-byte estimate undercounts by $\sim$10\%).

  $^{\dag}$\,Pure inference seconds per ensemble member per 15-day (360\,h)
  forecast (excludes data fetch, model load, container startup, zarr
  write), from the earth2studio per-call progress markers in the
  production-run logs. Atlas estimated from its sampler (60 rollout steps
  $\times$ 38 s/step).

  $^{\S}$\,\textbf{CS h} = wall-clock hours for one 10-member case-study
  ensemble on a single Alps node (4 GH200 in parallel):
  $10 \times \text{s/mbr} / 4 / 3600$.

  $^{\ddag}$\,Per production-baseline run (1120 forecasts). NH = node-hours;
  CHF at the Swiss academic tier-3 rate with the ETH discount
  (CHF 2.88 / node-hour); kWh at $\sim$4.8 kWh per node-hour delivered.

  $^{\star\star}$\,\textbf{aifs\_perturbed} is the deterministic AIFS
  checkpoint with the Phase 2 decoder perturbation ($\sigma=0.028$); its
  per-member time and memory match AIFS-ENS because it shares the identical
  AIFS forward graph (weights perturbed, not retrained). The 255\,M
  parameter count is from our checkpoint audit (not stated in the source
  paper).
\end{sidewaystable}
